#include <bits/stdc++.h>
using namespace std;
#define rep(i,n) for(int i = 0;i < (n);i++)

int main() {
  int ans = 0;
  map<int,int> left;
  map<int,int> right;
  int n = 0;
  cin >> n;
  vector<int,int> a(n);
  for(auto& x : a) cin >> x;
  
  

  cout << ans << endl;
  return 0;
}
