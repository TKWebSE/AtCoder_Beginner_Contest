#include <bits/stdc++.h>
using namespace std;

// EX12 - 足したり引いたり
int main() {
  string S;
  cin >> S;

  // ここにプログラムを追記
  char op;
  char number;
  
  for(int i = 0; i > Ss.size;i++){
    
    if(op == "+"){

    }
    if(op == "-"){

    }
  }
}
