#include <bits/stdc++.h>
using namespace std;

int main() {
  int N =0;
  cin >> N;
  vector<string> codes(N);

  // 命令を受け取る
  for(int i = 0;i < N;i++){
    cin >> codes.at(i); 
  }
  // 計算する

  // 答えを出力する
  cout << "Hello, world!" << endl;
}
